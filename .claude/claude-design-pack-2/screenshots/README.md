# Screenshots — drop them here

Optional but helpful. Take screenshots of these surfaces while dev is running and drop them in this folder with the suggested filenames. Claude Design uses them to anchor the visual language of the new surfaces against what's already shipped.

## Recommended

| Filename | What to capture | URL |
|---|---|---|
| `01-drill-in.png` | The match drill-in page (V3ScoreStrip, V3Timeline events, V3ClipPanel, V3MatchStats). Match Center round-trips with this — Claude Design needs to see what it links to. | `http://localhost:3000/coach/web/match/session_007` |
| `02-player-profile.png` | The player profile with the filmstrip visible. Match Center calendar reuses this filmstrip language. | `http://localhost:3000/coach/web/player/player_001` |
| `03-squad-pitch.png` | The squad pitch — the brand language Match Center should sit alongside. | `http://localhost:3000/coach/web/squad` |
| `04-record-wizard.png` | The existing 4-step Record / Prep wizard — Match Center's upcoming-match state folds this in (Step 2 "Attendance & Jerseys" is the densest screen, ideal to capture). | `http://localhost:3000/coach/web/record` |

## Optional but useful

| Filename | What to capture | Why |
|---|---|---|
| `05-bib-card-modal.png` | The Player Card share modal (open it from the player profile's top-right thumbnail button). | Shows the brand-aligned modal pattern — how cards float on indigo. |
| `06-compare-page.png` | The compare page with 2-3 players selected. | Shows the multi-player overlay radar pattern + chip rail. |
| `07-mobile-player-profile.png` | iPhone-width screenshot of the player profile. | Anchors the mobile design language Claude Design should mirror. |

Filenames don't have to be exact — Claude Design just needs visual context. If you only have time for one, take **01-drill-in.png** since that's the link target Match Center has to round-trip with.

If you skip screenshots entirely: the brief has descriptive paragraphs of these surfaces, so Claude Design can still build off the brief alone. Screenshots make the result tighter.
